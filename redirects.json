{
  "_comment": "Pages that have moved. Each key is the old path under dist/; each value is the new path, relative to the site root. The build writes a redirecting stub at the old path so a published link keeps working after a variant changes family or slug.",
  "chess/variants/blind-chess": "xiangqi/variants/banqi"
}
